# Aucune clé API n'est nécessaire : l'ECB Data Portal et Eurostat sont publics.
# Ce fichier n'est utile que si tu passes par un proxy d'entreprise.
# HTTPS_PROXY=http://user:pass@proxy.local:8080
# HTTP_PROXY=http://user:pass@proxy.local:8080
